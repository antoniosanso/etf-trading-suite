
# Signals config — SMA + ATR + Breakout defaults
strategy: "sma_crossover"   # puoi lasciarla: i campi breakout servono a Unified

# SMA params
sma_fast: 20
sma_slow: 50
min_periods: 20

# ATR params
atr_period: 14

# Breakout / Donchian (richiesti dal job Unified)
breakout_lookback: 260      # ~52 settimane
breakout_buffer_pct: 0.3    # piccolo buffer

# Altri default sicuri
risk_per_trade_pct: 1.0
output_dir: "./outputs/signals"
